# MATSU システムコード監査レポート

## 1. 概要
本レポートは、洛星文化祭チケットシステム「MATSU」のソースコード監査結果をまとめたものです。
システムは Django REST Framework (Backend) と Next.js (Frontend) で構成されており、入場枠管理、属性別の購入制限、動的フォーム、QRコードチェックインなどの機能を備えています。

## 2. 評価点 (Good Points)
本システムは全体的にモダンな設計思想で作られており、特にデータの整合性と拡張性において高い品質を保っています。

*   **排他制御の実装**: チケット購入処理 (`CheckoutRequestSerializer.create`) において `select_for_update()` を使用しており、同時アクセスによる在庫の過剰販売（オーバーブッキング）を適切に防いでいます。
*   **トランザクション管理**: 重要な更新処理は `transaction.atomic` でラップされており、エラー発生時のデータ不整合を防ぐ設計になっています。
*   **柔軟な属性設定**: `AttributeConfig` モデルにより、ユーザー属性（生徒、保護者、一般など）ごとの購入制限や入力フォーム定義をデータベース側で管理できる設計は非常に拡張性が高いです。
*   **型安全性**: Frontend は TypeScript と Zod を使用して厳格な型チェックとバリデーションを行っており、堅牢な作りになっています。

## 3. セキュリティ監査とリスク

### 3.1 設定ファイル (Settings)
*   **DEBUGモード**: 現在 `backend/core/settings.py` で `DEBUG` がデフォルトで `True` になっています。本番環境では必ず `False` に設定する必要があります。
*   **SECRET_KEY**: デフォルト値がハードコードされています。本番環境では強力なランダム文字列を環境変数から読み込むように徹底してください。
*   **ALLOWED_HOSTS / CORS**: デフォルトで localhost が許可されています。デプロイ時には実際のドメインのみを許可するように設定を変更する必要があります。

### 3.2 認証・認可
*   **JWTの保存場所**: 現在 Frontend ではアクセストークンを `localStorage` に保存しています。これは XSS（クロスサイトスクリプティング）攻撃に対して脆弱です。より高いセキュリティが必要な場合、HttpOnly Cookie への保存を検討してください。
*   **閲覧権限**: `EntrySlotViewSet` はデフォルトの `IsAuthenticated` 権限を使用しています。もしログイン前のユーザーに入場枠の空き状況を見せたい場合は、`AllowAny` に変更する必要があります。

## 4. コード品質とパフォーマンス

### 4.1 データベースクエリ
*   **N+1問題**: `TicketViewSet` では `select_related` が使用されており最適化されていますが、`ReservationViewSet` でチケット情報をネストして返す場合、N+1問題が発生する可能性があります。`prefetch_related` の使用を確認・検討してください。

### 4.2 エラーハンドリング
*   Frontend の `fetchApi` 関数は汎用的なエラーをスローしますが、ユーザーに表示されるエラーメッセージが「HTTP Error: 500」など技術的な内容になる可能性があります。ユーザーフレンドリーなメッセージへの変換処理を追加することを推奨します。

## 5. 具体的な改善策 (Action Items)

### 優先度: 高 (必須)
1.  **環境変数の整備**: 本番環境用に `.env` ファイルを作成し、以下の変数を設定する。
    *   `DEBUG=False`
    *   `DJANGO_SECRET_KEY=<強力なランダム文字列>`
    *   `ALLOWED_HOSTS=your-domain.com`
    *   `CORS_ALLOWED_ORIGINS=https://your-domain.com`
2.  **HTTPS化**: 本番運用時は必ず HTTPS 通信を行うようにサーバー（Nginx等）を設定する。

### 優先度: 中 (推奨)
1.  **認証フローの強化**: JWT を HttpOnly Cookie で管理する方式への移行を検討する。
2.  **ローディング表示**: データの取得中や処理中にスケルトンスクリーンやスピナーを表示し、UXを向上させる（現状も一部実装されていますが、全体的に適用する）。
3.  **ハードコード文字列の排除**: Backend のエラーメッセージ（日本語）を定数ファイルや翻訳ファイルに切り出し、管理しやすくする。

### 優先度: 低 (将来的な検討)
1.  **テストコードの拡充**: 特に同時購入時の排他制御が正しく動作するかを確認する並行テストを追加する。
2.  **ログ監視**: エラー発生時に管理者に通知が飛ぶようなログ監視の仕組み（Sentry等）を導入する。

## 6. 追加すると便利な機能提案 (Feature Suggestions)

### 6.1 来場者向け機能 (For Visitors)
*   **チケット譲渡機能**: 代表者が友人の分もまとめて購入した場合に、LINEやメールでチケット（QRコードURL）を分配できる機能。これにより、別々に入場することが可能になります。
*   **Apple Wallet / Google Wallet 対応**: QRコードをスマートフォンのウォレットアプリに追加できるようにし、当日のスムーズな入場を支援します。
*   **キャンセル待ち機能**: 満席の枠に対してキャンセル待ち登録を行い、空きが出たらメールで通知する機能。機会損失を防げます。
*   **リマインドメール**: 開催前日にQRコードのリンクと共にリマインドメールを自動送信する機能。当日のトラブルを減らせます。

### 6.2 管理者向け機能 (For Admins)
*   **CSVエクスポート**: 予約者リストやチェックイン状況をExcel/CSV形式でダウンロードする機能。名簿作成や、文化祭終了後の集計・分析に役立ちます。
*   **手動チェックイン**: スマートフォンを持っていなかったり、充電切れでQRコードが表示できない来場者のために、管理画面から名前や電話番号検索でチェックイン処理を行う機能。
*   **リアルタイム入場状況モニター**: 現在の入場者数、入場率などをリアルタイムでグラフ表示し、混雑状況を可視化する機能（ダッシュボードの強化）。
*   **緊急連絡一斉送信**: 台風やトラブルなどの緊急時に、特定の枠の予約者に対してメールを一斉送信する機能。

## 7. 結論
基本的な設計は非常にしっかりしており、特にチケットシステムで最も重要な「在庫管理」と「排他制御」が正しく実装されている点は高く評価できます。
上記の設定変更とセキュリティ対策を行えば、十分に本番運用に耐えうるシステムであると判断します。
