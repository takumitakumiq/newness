# 🔍 コードレビューレポート

**実施日:** 2026年1月1日
**対象:** MATSU システム (Backend: Django / Frontend: Next.js)

---

## 1. 全体評価

**評価: A (非常に良好)**

システムはモダンなアーキテクチャ（Next.js + Django REST Framework）で構築されており、文化祭チケットシステムとして必要な機能（定員管理、属性別フォーム、QR入場、譲渡機能など）が網羅されています。
特に、**排他制御による在庫管理**や**動的フォーム**の実装は、実際の運用課題を深く理解した設計となっており高く評価できます。

---

## 2. 詳細レビュー

### 🟢 良い点 (Strengths)

1.  **堅牢な在庫管理 (Backend)**
    *   `CheckoutView` において `select_for_update()` を使用した行ロックが行われており、高負荷時のダブルブッキング（定員オーバー）を確実に防ぐ設計になっています。
    *   `EntrySlot` モデルに `remaining` プロパティがあり、常に最新の残席数を計算できる状態です。

2.  **柔軟なフォーム設計 (Full Stack)**
    *   `AttributeConfig` モデルに `form_schema` (JSON) を持たせ、フロントエンドでそれを解釈してフォームを生成する仕組みは秀逸です。
    *   「中学生には学校名を聞く」「一般には聞かない」といった仕様変更に、コード修正なしで対応できます。

3.  **ユーザー体験への配慮 (Frontend)**
    *   `useCartStore` (Zustand) でカート情報を `localStorage` に永続化しており、ブラウザを閉じてもカートの中身が消えない設計はUXとして優れています。
    *   フロントエンド側でも `canAddItem` で在庫や購入上限の事前チェックを行っており、無駄なAPIリクエストを防いでいます。

4.  **機能の網羅性**
    *   単なる予約だけでなく、「チケット譲渡」「スタッフチャット」「プロモーションコード」など、実際の運営で欲しくなる機能が最初から組み込まれています。

### 🟡 改善の余地 (Improvements)

1.  **環境変数の管理**
    *   `backend/core/settings.py` で `SECRET_KEY` や `DEBUG` のデフォルト値が設定されていますが、本番環境では必ず `.env` から読み込むように徹底する必要があります。
    *   現状: `os.getenv('DEBUG', 'True')` -> 本番でうっかり `True` にならないよう注意。

2.  **エラーハンドリングの統一**
    *   フロントエンドの `fetchApi` はエラー時に `throw error` しますが、各コンポーネント側での `try-catch` が漏れていると、予期せぬクラッシュにつながる可能性があります。グローバルなエラーバウンダリの導入を検討しても良いでしょう。

3.  **テストコード**
    *   主要なロジック（特に在庫引き落としや譲渡フロー）に対する自動テスト（Unit Test / Integration Test）を充実させることで、今後の保守性がさらに向上します。

---

## 3. セキュリティチェック

*   **認証**: JWT (JSON Web Token) を使用しており、標準的かつ安全です。
*   **権限**: `IsAuthenticated` や `IsAdminUser` などのパーミッションクラスが適切に使用されているか、各Viewで再確認を推奨します。
*   **CORS**: `django-cors-headers` が導入されており、適切に設定されています。

---

## 4. 結論

**「即戦力として運用可能なレベル」** に仕上がっています。
ドキュメント（README.md等）も整備されたため、引き継ぎや今後の拡張もスムーズに行えるでしょう。

**推奨アクション:**
*   本番デプロイ前に、`DEBUG=False` での動作確認を行うこと。
*   データベースのバックアップ運用フローを確立すること。

以上
