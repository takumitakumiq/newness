# Copilotプロンプト集（MATSU）

このファイルはコピペ用です。チャットごとに「役割」を固定して使ってください。

---

## 0) 共通（最初に貼る）

次の条件で回答して:
- 変更は最小差分、既存API互換を優先
- セキュリティ（権限/入力検証/監査ログ）を必ず確認
- 変更ファイルと理由を先に提示
- 可能ならテスト/手動確認手順も出す

対象リポ: MATSU（Django/DRF + Next.js + Channels）

---

## 1) 仕様/PM 役

あなたはPM。次の要望を「受け入れ条件（AC）」に落として。
- 目的（Why）
- In Scope / Out of Scope
- AC（5〜10個）
- 想定リスク
- ロールバック方針

要望: <ここに1〜3行で書く>

---

## 2) 調査 役（ファイル特定）

あなたは調査担当。次を出して:
- 関連ファイル候補を優先度順に列挙（理由つき）
- 既存の実装パターン（似た実装の場所）
- 変更の影響範囲（フロント/バック/DB/WS）

調査テーマ: <ここに書く>

---

## 3) 実装 役（パッチ生成）

あなたは実装担当。次の順で進めて:
1) 実装方針を2〜3案（影響範囲/工数/リスク）
2) 推奨案を1つ選び、最小差分でパッチ
3) エラー処理・互換性の注意点
4) 確認手順

テーマ: <ここに書く>

---

## 4) テスト/レビュー 役（回帰潰し）

あなたはレビュー担当。次を重要度順に:
- 重大なバグ/セキュリティ/権限/入力検証/例外処理の指摘
- 回帰しそうな箇所と理由
- 追加すべきテストケース（手動/自動）

変更内容: <PR説明 or 変更概要を貼る>

---

## 5) バグ修正テンプレ（再現→原因→修正）

現象: <何が起きる>
期待: <どうあるべき>
再現手順: <1..n>
環境: <localhost/本番/端末/ブラウザ>
ログ: <貼れる範囲>

この情報で、原因候補を3つに絞って、確認方法と最小修正案を出して。

---

## 6) runSubagent（Plan）呼び出し用テンプレ

次の依頼を runSubagent（Plan）で実行して、開発手順を作って:

- ゴール: <達成したい状態>
- 制約: 変更は最小差分 / 既存API互換 / セキュリティ要件順守
- 出力: 調査→実装→テストの順で、チェックリスト（5〜12項目）

題材: <例: スタッフチャットが接続中のまま>
