# 機能追加 指示書（MATSU）

対象: MATSU（Django/DRF + Next.js + Channels）
作成日: 2026-01-01
最終更新: 2026-01-01

この指示書は、下記の機能群を実装するための「要件・設計・実装タスク・受け入れ条件」をまとめたものです。

---

## ✅ 実装完了一覧

### フェーズ1（現場の事故を減らす：最優先）
- [x] **2-B 二重スキャン防止** - 端末側で5秒間の短時間キャッシュ
- [x] **2-C スキャン音/バイブ** - 成功/失敗で音色変更、ON/OFF切り替え可能
- [x] **2-D 端末IDごとの集計** - `/api/admin/device-stats` で端末別処理件数表示
- [x] **3-A 譲渡リンク取り消し** - `/api/transfers/cancel` でPENDINGの譲渡をキャンセル
- [x] **5-C 緊急停止** - SystemSettingモデル + `/api/admin/emergency` + UI

### フェーズ2（運用効率）
- [x] **2-A オフラインチェックイン（batch API）** - `/api/checkin/batch` でまとめて同期
- [x] **2-E 例外処理モード** - 手動チェックインUI（`/staff/manual`）で名前検索→入場
- [x] **2-F 取り消しチェックイン** - `/api/admin/checkin/revert` で誤スキャン救済
- [x] **3-B 譲渡先指定** - `intended_email` フィールドで受取者限定
- [x] **3-D 受取側プレビュー** - `/api/transfers/preview` でチケット内容確認
- [x] **4-A 未読/既読** - `/api/chat/unread` と WebSocket `mark_read` イベント

---

## 0. 前提（共通）

### 0.1 環境・接続
- フロントは `NEXT_PUBLIC_API_URL` を参照して API / WebSocket 接続先を決定する。
- WebSocket: `ws(s)://<host>/ws/chat/?token=<JWT>`
- REST API: `http(s)://<host>/api/...`

### 0.2 セキュリティ/監査（共通要件）
- 操作者・実行者は**必ずサーバ側の `request.user` で確定**し、クライアント入力は信用しない。
- 重要操作（チェックイン/手動入場/譲渡キャンセル/譲渡期限延長/緊急停止）は監査ログに残す。
- 競合が起きる可能性がある操作は DB ロック（`select_for_update`）＋ロック後再検証を徹底。

### 0.3 実装方針（共通）
- 既存UI/挙動との互換性を崩さない（APIレスポンス形式を変える場合は段階移行を用意）。
- できる限り「小さなPR」に分割：
  1) DB/モデル変更 → 2) API追加 → 3) UI追加 → 4) 運用・テスト

---

## 1. チケット（QR）

### 1-A. QRの一括表示 / 一括ダウンロード（PDF/画像）

#### 要件
- 代表者（予約者）が自分の予約に紐づく複数チケットのQRを一覧表示できる。
- 同一覧を「PDFで一括ダウンロード」できる（最低限は印刷用レイアウトでも可）。

#### 設計案
- フロント: 予約詳細画面（マイページ）に「一括表示」「PDF出力」ボタンを追加。
- PDF生成方式（どちらか採用）
  - 案1（最小）: ブラウザの印刷機能（CSS print）で PDF 化を誘導
  - 案2: フロントで `pdf-lib` 等を用いクライアント生成
  - 案3: バックエンドでPDF生成（ReportLab等）→ `/api/reservations/{id}/tickets.pdf`

#### 受け入れ条件
- 予約1件に複数チケットがある場合、全てのQRが表示される。
- PDF/印刷出力で、各チケットに「氏名」「枠」「種別」「チケットID」が読める。

#### 実装タスク
- [ ] 予約詳細APIがチケット情報（ID/guest_info/slot/attribute）を返しているか確認
- [ ] QR生成を統一（既存表示があるならそれを再利用）
- [ ] 印刷CSS（ページ区切り、余白、拡大率）を調整
- [ ] （PDF生成採用の場合）ライブラリ選定・導入

---

### 1-B. Apple Wallet / Google Wallet 風のチケット表示（簡易）

#### 要件
- “チケットカード”の見た目を最適化し、現場で提示しやすい（大きい文字・高コントラスト）。
- 厳密なWallet連携（署名パス）は不要。まずは見た目・提示導線の改善。

#### 設計案
- 1枚表示専用のページを作成（例: `/mypage/tickets/{ticketId}`）
- 画面: QR（大） + 名前（大） + 枠（日時） + 種別

#### 受け入れ条件
- スマホでスクロールなしでQRが表示され、スタッフが読み取りやすい。

#### 実装タスク
- [ ] チケット単体取得API or 既存データから表示
- [ ] 表示最適化（フォントサイズ/余白/明暗）

---

### 1-C. 代表者が“同行者に配布”できる一括共有リンク（期限付き）

#### 要件
- 代表者が「この予約（または一部チケット）」を、同行者に共有できる。
- 共有リンクは期限付き・無効化可能。

#### 設計案
- 新規モデル `TicketShareLink`
  - `token`（ランダム）
  - `reservation` or `tickets`（対象）
  - `expires_at`
  - `revoked_at`（無効化）
  - `created_by`
- API
  - `POST /api/shares/create`（対象選択、期限）
  - `POST /api/shares/revoke`（無効化）
  - `GET /api/shares/{token}`（閲覧）
- 公開リンク閲覧は「閲覧のみ（QR表示のみ）」から開始する。

#### 受け入れ条件
- 期限切れ/無効化後はアクセスできない。
- 共有リンクで対象チケットのQRを表示できる。

#### 実装タスク
- [ ] DB追加 + migrate
- [ ] Token生成・レート制限
- [ ] 共有ページ（認証なし）実装

---

### 1-D. チケット名（ゲスト名）の表示最適化

#### 要件
- チケット一覧で「ゲスト名」を優先表示し、探しやすい。
- 空の場合のフォールバック（予約者名/「未入力」）を統一。

#### 実装タスク
- [ ] 表示ルールを決定（例: `guest_info.name` → `reservation.user_name` → `"未入力"`）
- [ ] 一覧・詳細・印刷の全表示箇所に適用

---

### 1-E. 端末にオフライン保存（電波が悪い会場向け）

#### 要件
- ユーザー端末で「自分のチケット（QR）」をオフラインで開ける。

#### 設計案
- PWA/Service Worker + IndexedDB
- 「オフライン保存」ボタンで対象データ（QR表示に必要な最小情報）を IndexedDB に保存

#### 受け入れ条件
- 機内モードでもチケット表示が開く。

---

## 2. チェックイン（現場運用）

### 2-A. オフラインチェックイン（端末に一時保存→復帰時同期）

#### 要件
- スキャン端末が一時的にオフラインでも入場処理を止めない。
- 復帰時にまとめて同期し、サーバ側で整合性を確定。

#### 設計案
- フロント（スタッフスキャン画面）:
  - オフライン時は「ローカル記録モード」
  - 端末に `ticket_uuid + scanned_at + device_id` をキュー保存（IndexedDB）
- API（新規）:
  - `POST /api/checkin/batch`（複数件）
  - レスポンス: 成功/失敗、既に入場済み等の理由
- サーバ:
  - 1件ずつ `select_for_update` + 既存チェックイン処理を流用

#### 受け入れ条件
- オフラインでも「仮入場」記録が残る。
- 復帰後の同期で最終結果が一覧で見える。

---

### 2-B. 二重スキャン防止（端末側でも短時間ロック）

#### 要件
- 同じQRを短時間に連続スキャンしたとき、端末側で弾く。

#### 設計案
- 端末側に `ticket_uuid -> last_scanned_at` の短期キャッシュ（例: 5〜10秒）

---

### 2-C. スキャン音/バイブ切り替え（成功/失敗で音色変更）

#### 要件
- 成功/失敗で音・バイブが変わる。
- 設定でオン/オフ可能。

---

### 2-D. 端末IDごとのログ集計（どのゲートが詰まってるか）

#### 要件
- 端末ID（ゲート）ごとの処理件数/失敗率/時間帯推移が見える。

#### 設計案
- 既存 `CheckInLog.device_id` を集計
- 管理画面API: `/api/admin/checkin/stats?from=&to=`

---

### 2-E. “例外処理モード”（名前検索→本人確認→手動入場）

#### 要件
- QRが出せない場合に、氏名/メールで検索→本人確認→入場処理。

#### 設計案
- 既存 `ManualCheckInView` をUIで使いやすくする（検索結果で即チェックイン）。

---

### 2-F. 取り消しチェックイン（誤スキャン救済、監査ログ付き）

#### 要件
- 誤って入場済みにしたチケットを「取り消し」できる。

#### 設計案
- API（管理者のみ）
  - `POST /api/admin/checkin/revert`（ticket_id、理由）
- サーバ
  - `ticket.status = valid` に戻す
  - 監査ログ `CheckInLog(action="revert")`

---

### 2-G. リアルタイム入場者数ダッシュボード（時間帯別）

#### 要件
- 直近5分/15分/1時間の入場者数がわかる。

#### 設計案
- WebSocketでメトリクス配信（または一定間隔でREST更新）

---

### 2-H. スタッフ権限レベル（スキャンのみ/手動可/管理可）

#### 要件
- 権限を段階化し、誤操作を減らす。

#### 設計案
- Django Group/Permission を導入
  - `can_scan`
  - `can_manual_checkin`
  - `can_admin_dashboard`

---

## 3. 譲渡

### 3-A. 譲渡リンクの取り消し（未受取ならキャンセル）

#### 要件
- 発行者が PENDING の譲渡リンクを取り消せる。

#### 設計案
- API: `POST /api/transfers/cancel`（token or transfer_id）
- サーバ: `status=CANCELLED`、監査ログ

---

### 3-B. 譲渡先を指定（メール/ユーザー名指定で誤受取防止）

#### 要件
- “誰でも受け取れる”をやめ、指定した相手だけ受け取れるようにする。

#### 設計案
- `TicketTransfer` に `intended_email` または `intended_user` を追加
- Accept時に一致チェック

---

### 3-C. 譲渡の受取期限延長

#### 要件
- 発行者が期限を延長できる（PENDINGのみ）。

#### 設計案
- API: `POST /api/transfers/extend`（+hours など）

---

### 3-D. 受取側の本人確認フロー（確認画面・チケット詳細表示）

#### 要件
- 受け取り前にチケット内容を表示して、誤受取を防ぐ。

#### 設計案
- `GET /api/transfers/preview?token=`
- 受取画面で詳細を表示 → 「受け取る」

---

### 3-E. 譲渡履歴のCSV出力（監査用）

#### 要件
- 期間指定で譲渡履歴をCSVダウンロード。

---

### 3-F. 譲渡時の通知（メール/画面通知）

#### 要件
- 譲渡作成時・受取時に通知。

---

## 4. スタッフチャット（WebSocket）

### 4-A. 既読/未読（未読数、誰が読んだか）

#### 要件
- 未読数がわかる。
- 既読者一覧（最低限はスタッフのみ）が見える。

#### 設計案
- 既存 `ChatMessageRead`（存在する前提）を利用
- WebSocket イベント
  - `read`: { message_id, read_at }
  - サーバは既読記録し、未読数更新を配信

---

### 4-B. 重要ピン留め

#### 要件
- 管理者がメッセージをピン留めできる。

#### 設計案
- `ChatMessage.is_pinned` 追加、または別テーブル

---

### 4-C. メンション（@staff、@admin）

#### 要件
- `@admin` 等の記法を解釈し、通知/ハイライト。

---

### 4-D. 役割別チャンネル

#### 要件
- 全体/入場口A/本部/救護/迷子 などチャンネル分け。

#### 設計案
- `ChatChannel` モデル + `ChatMessage.channel`
- WebSocket: `ws/chat/<channel>/`

---

### 4-E. 検索（キーワード/期間/ユーザー）

#### 要件
- 管理画面 or チャット画面で検索。

---

### 4-F. 添付（写真1枚だけでも）

#### 要件
- 画像1枚を添付可能。

#### 設計案
- まずは「アップロード→URL貼り付け」方式が最小

---

### 4-G. 定型文ボタン

#### 要件
- よく使う文言をワンタップ送信。

---

### 4-H. “配信”モード

#### 要件
- 管理者だけが全体アナウンス送信。

---

### 4-I. 通知（ブラウザ通知/バッジ/音）

#### 要件
- 画面を見ていなくても気づける。

---

## 5. 管理画面（運営）

### 5-A. 日別・枠別の売上/発券/入場数 可視化

#### 要件
- 日別/枠別で「販売数」「キャンセル数」「入場数」を一覧。

#### 設計案
- 集計APIを作り、管理画面でグラフ表示（最低限テーブルでも可）

---

### 5-B. 枠の定員変更の影響シミュレーション（残数再計算）

#### 要件
- 定員変更を保存前に、残数がどう変わるか確認できる。

---

### 5-C. 緊急停止（購入停止/譲渡停止/入場停止）

#### 要件
- 3つの停止フラグを切り替え。

#### 設計案
- `SystemSetting` モデル
  - `purchase_enabled`
  - `transfer_enabled`
  - `checkin_enabled`
- API/ミドルウェアで都度チェック

---

### 5-D. 監査ログビュー（誰がいつ何をしたか）

#### 要件
- 重要操作（チェックイン/取り消し/譲渡/停止切替）のログを一覧。

---

### 5-E. 設定のエクスポート/インポート

#### 要件
- 枠/属性/設定をJSONで出力・取り込み。

---

### 5-F. CSV一括インポート（枠/属性/スタッフ）

#### 要件
- CSVから枠/属性/スタッフを作成。

---

## 6. 実装優先度（おすすめロードマップ）

### フェーズ1（現場の事故を減らす：最優先）
- 2-B 二重スキャン防止
- 2-C 音/バイブ
- 2-D 端末ID集計
- 3-A 譲渡取り消し
- 5-C 緊急停止

### フェーズ2（ユーザー体験と配布）
- 1-A QR一括表示/印刷
- 1-C 一括共有リンク
- 1-D 表示最適化

### フェーズ3（オフライン/高度運用）
- 2-A オフラインチェックイン
- 1-E オフラインチケット
- 4-A 既読/未読
- 4-D チャンネル

---

## 7. 受け入れ試験（最低限）

- チケット
  - 一括表示で全チケットが出る
  - 印刷レイアウトが崩れない
- チェックイン
  - 二重スキャンが端末で弾かれる
  - 取り消しチェックインが監査ログに残る
- 譲渡
  - 取り消し後は受取不能
  - 期限切れは受取不能
- チャット
  - 既読/未読が表示される（導入時）
- 管理
  - 緊急停止ONで対象機能が確実に止まる

---

