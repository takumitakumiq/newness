# 👮‍♂️ 厳格コードレビューレポート

このファイルは「過去の厳格レビュー結果」と、今後の手戻りを減らすための「常用チェックリスト」をまとめたものです。

**実施日:** 2026年1月1日
**レビュアー:** GitHub Copilot (Strict Mode)
**対象:** MATSU システム

---

## 0. 開発・レビュー用チェックリスト（常用）

### 0.1 PR前セルフチェック
- **権限/操作者**: 重要操作は `request.user` 起点で確定している（ボディの `user_id` / `operator` などを信用していない）。
- **競合/整合性**: 在庫・枠・譲渡・チェックイン等は `transaction.atomic()` + `select_for_update()` + ロック後再検証。
- **状態チェック**: 対象が「有効な状態」かを必ず検証（例: 譲渡受取時に元チケットが `valid` か）。
- **ページネーション**: 一覧系API/UIは無制限取得をしない（DRF Pagination / 無限スクロール等）。
- **設定の安全性**: `DEBUG` / `ALLOWED_HOSTS` / CORS / Cookie/JWT 設定が想定通り。
- **エラー表示**: APIエラーで画面が落ちない（UI側で `try/catch`、空状態/エラー状態の導線）。
- **最小差分**: ついでのリファクタや命名変更を入れていない。

### 0.2 レビュー時チェック（差し戻し基準）
- **監査ログ**: 誰が何をしたか追える（重要操作はログに残る・偽装できない）。
- **不正操作耐性**: 直接APIを叩かれても権限/状態/期限で防げる。
- **負荷見積もり**: N+1、無制限取得、重い集計が同期処理になっていない。
- **受け入れ条件**: FEATURE_INSTRUCTION の受け入れ条件が満たされている。

---

## 1. 総合判定

**判定: B- (要修正)**

「機能は動くが、本番運用にはリスクが残る」状態です。
前回のレビューでは「機能の網羅性」を評価しましたが、今回は「悪意ある操作」や「高負荷時の挙動」を想定して厳しくチェックしました。その結果、**セキュリティ上の懸念**と**データ整合性の不備**が見つかりました。

---

## 2. クリティカルな指摘事項 (Critical Issues)

### 🚨 1. チケット譲渡の抜け穴 (Security / Logic)
**場所:** `backend/api/views.py` (`TicketTransferAcceptView`)
**問題:**
譲渡を受け取る際、**「元のチケットがまだ有効か？」のチェックが漏れています。**
**シナリオ:**
1. 悪意あるユーザーAが、チケットの譲渡リンクを作成。
2. ユーザーAがそのチケットを自分でキャンセルする（返金を受ける）。
3. ユーザーAが譲渡リンクをユーザーBに送る。
4. ユーザーBがリンクを踏むと、**「キャンセル済みの無効なチケット」を受け取ってしまう。**
**修正案:**
`TicketTransferAcceptView` または `Serializer` 内で、`transfer.ticket.status == 'valid'` であることを必ず確認してください。

### 🚨 2. チャット機能のパフォーマンス爆弾 (Performance)
**場所:** `backend/api/views.py` (`ChatMessageView`)
**問題:**
`ChatMessage.objects...[:100]` となっていますが、ページネーション（「もっと見る」機能）が実装されていません。
チャットが盛り上がると、**過去のメッセージが永遠に閲覧不能**になります。また、ポーリング間隔によってはサーバー負荷が急増します。
**修正案:**
Django REST Frameworkの `LimitOffsetPagination` または `CursorPagination` を導入してください。

### 🚨 3. 監査ログの信頼性欠如 (Security)
**場所:** `backend/api/views.py` (`CheckInView`, `ManualCheckInView`)
**問題:**
チェックイン時の `operator`（操作者名）を、リクエストボディからそのまま受け取っています。
```python
operator = request.data.get('operator', request.user.username)
```
これでは、APIを直接叩けば**「他人の名前でチェックイン記録を偽装」**できてしまいます。
**修正案:**
`request.user.username` を強制的に使用し、クライアントからの入力は無視するか、あくまで「参考情報」として別フィールドに保存すべきです。

---

## 3. 改善すべき点 (Major Improvements)

### ⚠️ 1. 環境設定の脆弱さ
`backend/core/settings.py` において、`DEBUG` モードの判定が文字列比較のみで行われており、環境変数の設定ミスで意図せずデバッグモードが有効になるリスクがあります。
また、`ALLOWED_HOSTS` のデフォルトが緩すぎます。

### ⚠️ 2. エラーハンドリングの甘さ
フロントエンドの `api.ts` ではエラーを `throw` していますが、UI側（コンポーネント）で適切に `try-catch` されていない箇所が見受けられます。APIエラー時に画面が真っ白になる（クラッシュする）可能性があります。

### ⚠️ 3. テストコードの欠如
これほど複雑なロジック（特に在庫ロックや譲渡フロー）を持つシステムにもかかわらず、**自動テストが見当たりません。**
「動いているからヨシ」は、将来の修正でバグを埋め込む最大の要因です。

---

## 4. 推奨修正プラン

1.  **緊急修正**: `TicketTransferAcceptView` にチケット状態チェックを追加。
2.  **セキュリティ強化**: `CheckInView` の `operator` を `request.user` から取得するように変更。
3.  **設定見直し**: 本番環境用の `settings_prod.py` を作成し、環境変数を厳格化。
4.  **テスト作成**: 少なくとも `CheckoutView` と `TicketTransfer` 関連の単体テストを作成。

---

**総評:**
アイデアと機能実装力は素晴らしいですが、「システムを壊そうとする人」への防御力が不足しています。本番公開前に上記の修正を強く推奨します。
